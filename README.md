Based on https://github.com/kerwynrg/typeorm

## Compiling:

```sh
npm install
npm run compile
npm run package
```

To use firebird you need to install node-firebird
The generated package will be available on `build/package`.
